"""WAInsight export pipelines.

Modules:
    viewer_bundle_exporter — folder-in-ZIP offline HTML viewer
        (manifest.js + per-conv JSONP shards + media/ +
        self-contained index.html with virtual scroll, hash
        routing, and full-text search).
"""
