"""
WAInsight forensic report generators.

Produces self-contained HTML reports with embedded CSS, charts, and base64 images.
No external dependencies required — pure Python + SQL + HTML/CSS.
"""
